import { css } from "@emotion/react";

export const homeLandingWrapperCss = css`
  height: 100vh;
  width: 100%;
  position: sticky;
  top: 0;
  left: 0;
  overflow: hidden;
  z-index: var(--z-lowest);
`;
